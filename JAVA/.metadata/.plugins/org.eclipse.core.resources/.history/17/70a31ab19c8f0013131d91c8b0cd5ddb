package abstractDataTypes;

public class AVLTree<E extends Comparable<E>> {

	private AVLNode<E> root;
	
	public AVLTree() {
		root = null;
	}
	
	public void insert(E data) {
		AVLNode<Comparable<E>> node = new AVLNode<Comparable<E>>();
		if(root == null) {
			root = data;
		}
	}
}
